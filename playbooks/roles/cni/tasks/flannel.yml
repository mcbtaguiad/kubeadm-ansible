---
- name: Deploy Flannel CNI
  kubernetes.core.k8s:
    state: present
    src: https://raw.githubusercontent.com/flannel-io/flannel/{{ FLANNEL_VERSION }}/Documentation/kube-flannel.yml
    kubeconfig: "{{ KUBECONFIG }}"
  run_once: true
  delegate_to: "{{ groups['master'][0] }}"
